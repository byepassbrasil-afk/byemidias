# Signing config for release builds
# Generate a keystore: keytool -genkey -v -keystore byemidias-release.jks -keyalg RSA -keysize 2048 -validity 10000 -alias byemidias

# Add to local.properties (not committed to git):
# RELEASE_STORE_FILE=../byemidias-release.jks
# RELEASE_STORE_PASSWORD=your_store_password
# RELEASE_KEY_ALIAS=byemidias
# RELEASE_KEY_PASSWORD=your_key_password
